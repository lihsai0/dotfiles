# 通用 git 配置（入库，软链后立即生效）
# 个人身份信息在 config.local（不入库）: cp config_local_tpl config.local 后填写

[include]
    path = ~/.config/git/config.local

[core]
    excludesfile = ~/.config/git/ignore_global
    pager = delta

[init]
    defaultBranch = main

[push]
    autoSetupRemote = true

[interactive]
    diffFilter = delta --color-only

[merge]
    conflictStyle = zdiff3

# 个人多身份配置: ~/Workspaces 存在且已链接 config_workspaces 时生效
[includeIf "gitdir:~/Workspaces"]
    path = ~/Workspaces/.gitconfig
;[includeIf "gitdir:~/Workspaces/company/"]
;    path = ~/Workspaces/company/.gitconfig

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true

[delta]
    navigate = true

[url "ssh://git@github.com/"]
    insteadOf = https://github.com/
